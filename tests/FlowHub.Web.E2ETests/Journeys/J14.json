{
  "id": "J14",
  "category": "functional",
  "description": "Clicking a row in the Captures grid opens that capture's detail page",
  "entryUrl": "/captures",
  "preconditions": ["At least one capture exists"],
  "steps": ["Click the first row in the captures grid"],
  "expected": ["URL changes to /captures/{guid}"],
  "passes": true
}
